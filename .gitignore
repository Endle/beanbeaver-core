/target

# OCR models are provisioned out-of-band (CI downloads them from the
# ocr-models-v1 release; devs run scripts to fetch). Not vendored — too large.
# See docs/ocr-models.md for the pinned release and filenames.
/models/*.onnx
