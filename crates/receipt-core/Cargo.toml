[package]
name = "receipt-core"
version = "0.1.0"
edition = "2021"
publish = false
license = "MIT"

[lib]
name = "receipt_core"

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
